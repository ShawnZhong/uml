CONFIG_64BIT=y
CONFIG_BLK_DEV_INITRD=y # For booting test programs in an initrd.
CONFIG_BLK_DEV=y
CONFIG_SMP=y # Nearly every computer is SMP these days, so let’s build for testing on SMP.
CONFIG_PCI=y # Virtio all runs over fake PCI devices
CONFIG_ELF=y
CONFIG_BINFMT_SCRIPT=y # We really need ELF and shebang support.
CONFIG_NET=y
CONFIG_UNIX=y
CONFIG_PACKET=y # dhclient needs CONFIG_PACKET
CONFIG_INET=y # TCP/IP
CONFIG_NETDEVICES=y
CONFIG_VIRTIO_NET=y
CONFIG_NET_9P=y # Virtual 9P networking device
CONFIG_VIRTIO_PCI=y # Support virtio PCI devices
CONFIG_NET_9P_VIRTIO=y
CONFIG_9P_FS=y # Support 9P-based filesystem exports.
CONFIG_EXT4_FS=y
CONFIG_EXT4_USE_FOR_EXT23=y #			 ext2/3/4 support for the root
CONFIG_TMPFS=y # Needed for udev
CONFIG_INOTIFY_USER=y # Needed for udev
CONFIG_VIRTIO_BLK=y # Needed for the virtio HDD
CONFIG_VIRTIO_CONSOLE=y # Needed for the virtio console
CONFIG_DEBUG_KERNEL=y
CONFIG_DEBUG_INFO=y

CONFIG_KPROBES=y
CONFIG_DEBUG_INFO_DWARF5=y
CONFIG_DEBUG_INFO_REDUCED=y
CONFIG_DEBUG_INFO_COMPRESSED_NONE=y
CONFIG_DEBUG_INFO_SPLIT=y
CONFIG_GDB_SCRIPTS=y
CONFIG_SCHED_DEBUG=y
CONFIG_NO_HZ_FULL=y
CONFIG_RCU_NOCB_CPU=y
